# ⚠️  IMPORTANT — READ BEFORE INSTALLING  ⚠️
#
# There is ONE conflict in this stack that CANNOT be resolved by any
# requirements.txt change alone:
#
#   crewai==0.130.0  →  opentelemetry>=1.30  →  protobuf>=5.0
#   google-ai-generativelanguage==0.6.4       →  protobuf<5.0
#
# These two are hard-incompatible at these versions. You have two options:
#
#   OPTION A (recommended): Upgrade the four Google SDK packages that
#   require protobuf<5 to newer versions that support protobuf>=5.
#   This file already sets those minimum bounds below.
#
#   OPTION B: Downgrade crewai to <=0.86.0 (uses opentelemetry<1.30,
#   works with protobuf<5). Only if crewai==0.130.0 is negotiable.
#
# Everything else in this file is fully conflict-free.
# ─────────────────────────────────────────────────────────────────────────────

# ── CrewAI (pinned as required) ───────────────────────────────────────────────
crewai==0.130.0
crewai-tools==0.47.1

# ── Click (crewai-tools needs >=8.1.8) ───────────────────────────────────────
click>=8.1.8

# ── Pydantic v2 ───────────────────────────────────────────────────────────────
pydantic>=2.7,<3.0
# pydantic_core is auto-resolved — do NOT pin it separately

# ── OpenAI ────────────────────────────────────────────────────────────────────
openai>=1.50.0

# ── LangChain (unpinned — crewai 0.130.0 resolves to ~0.3.x) ─────────────────
langsmith>=0.1.67

# ── Google Cloud / AI ─────────────────────────────────────────────────────────
# These four are upgraded from your original pins because their old versions
# require protobuf<5, which conflicts with crewai 0.130.0 → otel>=1.30 → protobuf>=5
google-ai-generativelanguage>=0.6.10
google-api-core>=2.15.0,<3.0.0
google-api-python-client>=2.131.0
google-auth>=2.29.0
google-auth-httplib2>=0.2.0
google-cloud-aiplatform>=1.60.0
google-cloud-bigquery>=3.23.1
google-cloud-core>=2.4.1
google-cloud-resource-manager>=1.13.0
google-cloud-storage>=2.16.0
google-crc32c>=1.5.0
google-generativeai>=0.7.0
google-resumable-media>=2.7.0
googleapis-common-protos>=1.65.0

# ── Protobuf (must be >=5 for opentelemetry>=1.30 + crewai 0.130.0) ──────────
protobuf>=5.0.0,<6.0.0

# ── OpenTelemetry (all unpinned — resolved together as a matched group) ───────
opentelemetry-api
opentelemetry-sdk
opentelemetry-proto
opentelemetry-exporter-otlp-proto-common
opentelemetry-exporter-otlp-proto-grpc
opentelemetry-exporter-otlp-proto-http
opentelemetry-instrumentation
opentelemetry-instrumentation-asgi
opentelemetry-instrumentation-fastapi
opentelemetry-semantic-conventions
opentelemetry-util-http

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.110.3

# ── Data / ML ─────────────────────────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.2
onnxruntime>=1.18.0

# ── Misc ──────────────────────────────────────────────────────────────────────
Jinja2==3.1.4
jsonschema==4.22.0
oauthlib==3.2.2
pillow==10.3.0



pdfminer.six>=20221105
python-multipart>=0.0.22